<Window x:Class="QuanLyHangHoa.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:QuanLyHangHoa.ViewModels"
        mc:Ignorable="d"
        Title="Đăng Nhập - Inventory Pro Max" Height="560" Width="420"
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        x:Name="LoginWindow">

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <!-- Override global TextBox style: use outlined with floating hint, no clear button -->
        <Style x:Key="LoginTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
            <Setter Property="materialDesign:TextFieldAssist.HasClearButton" Value="False"/>
            <Setter Property="materialDesign:HintAssist.IsFloating" Value="True"/>
            <Setter Property="materialDesign:HintAssist.FloatingScale" Value="0.85"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Height" Value="Auto"/>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BodyBgBrush}" CornerRadius="20" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" Color="#20000000" Direction="-90" ShadowDepth="5" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Decorative Bar -->
            <Border Grid.Row="0" Height="6" Background="{StaticResource PrimaryIndigoBrush}" CornerRadius="20,20,0,0"/>

            <Grid Grid.Row="1" Margin="32,20,32,28">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Close -->
                    <RowDefinition Height="Auto"/> <!-- Header -->
                    <RowDefinition Height="*"/>    <!-- Form -->
                    <RowDefinition Height="Auto"/> <!-- Error -->
                    <RowDefinition Height="Auto"/> <!-- Actions -->
                </Grid.RowDefinitions>

                <!-- Close Button -->
                <Button Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top" 
                        Style="{StaticResource MaterialDesignIconForegroundButton}"
                        Width="32" Height="32"
                        Click="BtnClose_Click" Margin="0,-8,-16,0">
                    <materialDesign:PackIcon Kind="Close" Width="18" Height="18" Foreground="{StaticResource TextSecondaryBrush}"/>
                </Button>

                <!-- Brand Header -->
                <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,24">
                    <Border Width="64" Height="64" Background="{StaticResource SidebarActiveBrush}" CornerRadius="16" Margin="0,0,0,12">
                        <materialDesign:PackIcon Kind="Warehouse" Width="36" Height="36" Foreground="{StaticResource PrimaryIndigoBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="INVENTORY" FontSize="22" FontWeight="Black" Foreground="{StaticResource PrimaryIndigoBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="ĐĂNG NHẬP HỆ THỐNG" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Login Form -->
                <StackPanel Grid.Row="2" VerticalAlignment="Center">
                    <TextBox Style="{StaticResource LoginTextBoxStyle}"
                             materialDesign:HintAssist.Hint="Tên đăng nhập"
                             materialDesign:HintAssist.IsFloating="True"
                             Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

                    <PasswordBox x:Name="txtPassword"
                                 Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                                 materialDesign:HintAssist.Hint="Mật khẩu"
                                 materialDesign:HintAssist.IsFloating="True"
                                 materialDesign:HintAssist.FloatingScale="0.85"
                                 PasswordChanged="TxtPassword_PasswordChanged"
                                 FontSize="15" Margin="0,0,0,8"/>
                </StackPanel>

                <!-- Error Message -->
                <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="#EF5350" FontSize="12" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Margin="0,8,0,8" TextWrapping="Wrap" TextAlignment="Center"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

                <!-- Actions -->
                <StackPanel Grid.Row="4" Margin="0,8,0,0">
                    <Button Content="ĐĂNG NHẬP" FontSize="15" FontWeight="Bold"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Height="48"
                            materialDesign:ButtonAssist.CornerRadius="12"
                            Command="{Binding LoginCommand}" 
                            CommandParameter="{Binding ElementName=LoginWindow}"/>
                    
                    <TextBlock Text="Quên mật khẩu? Liên hệ quản trị viên" 
                               FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" 
                               HorizontalAlignment="Center" Margin="0,16,0,0"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
