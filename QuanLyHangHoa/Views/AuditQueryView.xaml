<UserControl x:Class="QuanLyHangHoa.Views.AuditQueryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#111827">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding ReportTitle}" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="#1F2937" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Text="San pham" Foreground="White" Margin="0,0,0,6"/>
                    <ComboBox ItemsSource="{Binding AvailableProducts}"
                              SelectedItem="{Binding SelectedProduct}"
                              DisplayMemberPath="Name"
                              Background="#374151"
                              Foreground="Black"
                              Padding="8"/>
                </StackPanel>
                <Button Grid.Column="1" Content="Xem lich su SP" Command="{Binding LoadProductLedgerCommand}" Background="#2563EB" Foreground="White" Padding="14,8" BorderThickness="0" VerticalAlignment="Bottom" Margin="0,0,20,0"/>

                <StackPanel Grid.Column="2" Margin="0,0,12,0">
                    <TextBlock Text="DocumentId" Foreground="White" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding DocumentIdText, UpdateSourceTrigger=PropertyChanged}" Background="#374151" Foreground="White" Padding="8"/>
                </StackPanel>
                <Button Grid.Column="3" Content="Xem timeline" Command="{Binding LoadDocumentTimelineCommand}" Background="#10B981" Foreground="White" Padding="14,8" BorderThickness="0" VerticalAlignment="Bottom"/>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Entries}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Background="#1F2937"
                  Foreground="Black"
                  RowBackground="#374151"
                  AlternatingRowBackground="#1F2937">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Loai" Binding="{Binding Kind}" Width="0.8*"/>
                <DataGridTextColumn Header="Thoi gian" Binding="{Binding OccurredAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="1.1*"/>
                <DataGridTextColumn Header="Hanh dong" Binding="{Binding Action}" Width="1*"/>
                <DataGridTextColumn Header="User" Binding="{Binding UserId}" Width="0.6*"/>
                <DataGridTextColumn Header="Product" Binding="{Binding ProductId}" Width="0.7*"/>
                <DataGridTextColumn Header="Warehouse" Binding="{Binding WarehouseId}" Width="0.8*"/>
                <DataGridTextColumn Header="SL" Binding="{Binding Quantity}" Width="0.5*"/>
                <DataGridTextColumn Header="DocumentId" Binding="{Binding DocumentId}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Background="#1F2937" CornerRadius="8" Padding="14" Margin="0,16,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#D1D5DB"/>
        </Border>
    </Grid>
</UserControl>
