<UserControl x:Class="QuanLyHangHoa.Views.ChangePasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:QuanLyHangHoa.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{StaticResource AppBackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:Card Grid.Row="0" Margin="0,0,0,24" UniformCornerRadius="12" materialDesign:ElevationAssist.Elevation="Dp2" Padding="16,8">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="Key" Width="28" Height="28" VerticalAlignment="Center" Foreground="{StaticResource AppTertiaryBrush}"/>
                <TextBlock Text="ĐỔI MẬT KHẨU" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="{StaticResource AppTertiaryBrush}"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Main Content -->
        <materialDesign:Card Grid.Row="1" UniformCornerRadius="12" materialDesign:ElevationAssist.Elevation="Dp2" Padding="32" MaxWidth="500" HorizontalAlignment="Left">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,32">
                    <materialDesign:PackIcon Kind="AccountCircle" Width="24" Height="24" Foreground="{StaticResource AppTertiaryBrush}" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding Username, StringFormat='Tài khoản: {0}'}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="{StaticResource AppTertiaryBrush}"/>
                </StackPanel>

                <!-- Using TextBox for compatibility with existing ViewModel [ObservableProperty] bindings -->
                <TextBox materialDesign:HintAssist.Hint="Mật khẩu hiện tại" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         materialDesign:TextFieldAssist.HasClearButton="True"
                         Margin="0,0,0,20" FontSize="14"
                         Text="{Binding CurrentPassword, UpdateSourceTrigger=PropertyChanged}"/>
                
                <TextBox materialDesign:HintAssist.Hint="Mật khẩu mới" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         materialDesign:TextFieldAssist.HasClearButton="True"
                         Margin="0,0,0,20" FontSize="14"
                         Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}"/>
                
                <TextBox materialDesign:HintAssist.Hint="Xác nhận mật khẩu mới" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         materialDesign:TextFieldAssist.HasClearButton="True"
                         Margin="0,0,0,32" FontSize="14"
                         Text="{Binding ConfirmPassword, UpdateSourceTrigger=PropertyChanged}"/>

                <Button Content="CẬP NHẬT MẬT KHẨU" 
                        Command="{Binding ChangePasswordCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}" 
                        Height="48" materialDesign:ButtonAssist.CornerRadius="12"
                        Margin="0,0,0,24"/>

                <TextBlock Text="{Binding StatusMessage}" 
                           Foreground="{StaticResource AppTertiaryBrush}" 
                           TextWrapping="Wrap" TextAlignment="Center" FontWeight="SemiBold"/>
                
                <Border BorderBrush="{StaticResource AppTertiaryBrush}" BorderThickness="0,1,0,0" Margin="0,24,0,0" Padding="0,16,0,0">
                    <TextBlock Text="Lưu ý: Để đảm bảo an toàn, hãy sử dụng mật khẩu mạnh và không chia sẻ cho người khác." 
                               FontSize="12" Foreground="#64748B" TextWrapping="Wrap" FontStyle="Italic"/>
                </Border>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>

