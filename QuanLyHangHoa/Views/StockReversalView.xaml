<UserControl x:Class="QuanLyHangHoa.Views.StockReversalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid Margin="{StaticResource PagePadding}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Row 0: Header Section -->
            <Grid Grid.Row="0" Margin="{StaticResource SectionBottomMargin}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="History" Width="32" Height="32" VerticalAlignment="Center" Foreground="{StaticResource AppTertiaryBrush}"/>
                    <StackPanel Margin="16,0,0,0">
                        <TextBlock Text="ĐẢO CHỨNG TỪ KHO" Style="{StaticResource TypographyH2}" Foreground="{StaticResource AppTertiaryBrush}"/>
                        <TextBlock Text="Hủy bỏ hiệu lực của chứng từ bằng cách tạo giao dịch đảo chiều" Style="{StaticResource TypographyCaption}" Opacity="0.7"/>
                    </StackPanel>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="Alert" Foreground="{StaticResource DangerTextBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Cần thận trọng khi thực hiện" Style="{StaticResource TypographyLabel}" Foreground="{StaticResource DangerTextBrush}" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- Row 1: Action Card -->
            <materialDesign:Card Grid.Row="1" Margin="{StaticResource Margin.Bottom.md}" Style="{StaticResource AppCard}" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,16,0">
                        <TextBlock Text="MÃ CHỨNG TỪ CẦN ĐẢO" Style="{StaticResource TypographyCaption}" Margin="0,0,0,6" Foreground="{StaticResource AppPrimaryBrush}"/>
                        <TextBox Height="42" Style="{StaticResource AppTextBoxStyle}"
                                 materialDesign:HintAssist.Hint="DocumentId (VD: PIN-2024-001)"
                                 Text="{Binding DocumentIdText, UpdateSourceTrigger=PropertyChanged}"
                                 Background="{StaticResource SurfaceMutedBrush}">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding ReverseDocumentCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,16,0">
                        <TextBlock Text="LÝ DO ĐẢO CHỨNG TỪ" Style="{StaticResource TypographyCaption}" Margin="0,0,0,6" Foreground="{StaticResource AppPrimaryBrush}"/>
                        <TextBox Height="42" Style="{StaticResource AppTextBoxStyle}"
                                 materialDesign:HintAssist.Hint="Nhập lý do chi tiết..."
                                 Text="{Binding Reason, UpdateSourceTrigger=PropertyChanged}"
                                 Background="{StaticResource SurfaceMutedBrush}"/>
                    </StackPanel>

                    <Button Grid.Column="2" Command="{Binding ReverseDocumentCommand}" 
                            Style="{StaticResource AppDangerButton}"
                            Height="42" Padding="24,0" VerticalAlignment="Bottom">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="HistoryOff" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="ĐẢO CHỨNG TỪ" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </materialDesign:Card>

            <!-- Row 2: Notice Card -->
            <materialDesign:Card Grid.Row="2" Style="{StaticResource AppCard}" Padding="24" VerticalAlignment="Top">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <materialDesign:PackIcon Kind="AlertCircle" Foreground="{StaticResource DangerTextBrush}" VerticalAlignment="Center" Width="28" Height="28"/>
                        <TextBlock Text="LƯU Ý QUAN TRỌNG" Style="{StaticResource TypographyH2}" Foreground="{StaticResource DangerTextBrush}" Margin="16,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <TextBlock Text="Chức năng này sẽ tạo các bản ghi điều chỉnh (Adjustment) đảo chiều toàn bộ giao dịch của chứng từ đã chọn và cập nhật ngay lập tức vào tồn kho thực tế." 
                               Style="{StaticResource TypographyBody}" TextWrapping="Wrap" Margin="0,0,0,24" LineHeight="24"/>
                    
                    <Border Background="{StaticResource SurfaceMutedBrush}" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="1" Padding="20" CornerRadius="{StaticResource Radius.md}">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <materialDesign:PackIcon Kind="CheckCircleOutline" Foreground="{StaticResource AppSuccessBrush}" VerticalAlignment="Center" Margin="0,0,12,0" Width="18" Height="18"/>
                                <TextBlock Text="Chỉ hỗ trợ đảo chứng từ của một kho duy nhất." Style="{StaticResource TypographyBody}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="CheckCircleOutline" Foreground="{StaticResource AppSuccessBrush}" VerticalAlignment="Center" Margin="0,0,12,0" Width="18" Height="18"/>
                                <TextBlock Text="Hãy tra cứu kỹ Mã chứng từ trong mục 'Truy vấn Kho' trước khi thực hiện." Style="{StaticResource TypographyBody}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <!-- Status Banner -->
                    <Border Margin="0,20,0,0" Background="{StaticResource SurfaceMutedBrush}" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="1" Padding="16,12" CornerRadius="{StaticResource Radius.md}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Information" Foreground="{StaticResource AppTertiaryBrush}" VerticalAlignment="Center" Width="18" Height="18"/>
                            <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource TypographyLabel}" Foreground="{StaticResource AppTertiaryBrush}" Margin="12,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>
