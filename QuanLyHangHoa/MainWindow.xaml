<Window x:Class="QuanLyHangHoa.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Phần Mềm Quản Lý Hàng Hoá - Pro Max Edition" Height="900" Width="1440"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI"
        Background="{StaticResource BodyBgBrush}">

    <Window.Resources>
        <Style x:Key="SidebarItemStyle" TargetType="RadioButton" BasedOn="{x:Null}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="8,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="12,0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" 
                                                         Width="20" Height="20" VerticalAlignment="Center" 
                                                         Foreground="{TemplateBinding Foreground}"/>
                                <ContentPresenter VerticalAlignment="Center" Margin="12,0,0,0"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource SidebarActiveBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource PrimaryIndigoBrush}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F1F3F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="GroupName" Value="SidebarMenu"/>
        </Style>

        <Style x:Key="SidebarSectionHeader" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#A0AEC0"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="16,24,0,8"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBgBrush}" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Brand/Logo -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="24,0">
                    <materialDesign:PackIcon Kind="ViewQuilt" Width="32" Height="32" Foreground="{StaticResource PrimaryIndigoBrush}"/>
                    <TextBlock Text="INVENTORY" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="ExtraBold" FontSize="20" VerticalAlignment="Center" Margin="12,0,0,0"/>
                </StackPanel>

                <!-- Navigation Menu -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
                    <StackPanel Margin="8,0">
                        <TextBlock Text="TỔNG QUAN" Style="{StaticResource SidebarSectionHeader}"/>
                        <RadioButton Content="Dashboard" Tag="ViewDashboard" Style="{StaticResource SidebarItemStyle}" IsChecked="True" Command="{Binding OpenDashboardCommand}"/>
                        
                        <TextBlock Text="KHO HÀNG" Style="{StaticResource SidebarSectionHeader}"/>
                        <RadioButton Content="Sản Phẩm" Tag="PackageVariant" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenProductViewCommand}"/>
                        <RadioButton Content="Nhập Kho" Tag="ArrowDownBox" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenStockInViewCommand}"/>
                        <RadioButton Content="Xuất Kho" Tag="ArrowUpBox" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenStockOutViewCommand}"/>
                        <RadioButton Content="Điều Chỉnh" Tag="Tune" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenStockAdjustmentViewCommand}"/>
                        <RadioButton Content="Kiểm Kê" Tag="ClipboardCheck" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenStockCountViewCommand}"/>

                        <TextBlock Text="HÓA ĐƠN" Style="{StaticResource SidebarSectionHeader}"/>
                        <RadioButton Content="Hóa Đơn Bán" Tag="FileDocumentOutline" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenSalesInvoiceViewCommand}"/>
                        <RadioButton Content="Hóa Đơn Mua" Tag="FileImportOutline" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenPurchaseInvoiceViewCommand}"/>
                        <RadioButton Content="Thanh Toán" Tag="CashMultiple" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenInvoicePaymentViewCommand}"/>
                        <RadioButton Content="Báo Cáo Công Nợ" Tag="AccountClock" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenDebtReportViewCommand}"/>

                        <TextBlock Text="BẢO HÀNH" Style="{StaticResource SidebarSectionHeader}"/>
                        <RadioButton Content="Quản Lý Bảo Hành" Tag="ShieldCheck" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenWarrantyViewCommand}"/>

                        <TextBlock Text="DANH MỤC" Style="{StaticResource SidebarSectionHeader}"/>
                        <RadioButton Content="Thương Hiệu" Tag="Label" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenBrandViewCommand}"/>
                        <RadioButton Content="Phân Loại" Tag="Shape" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenCategoryViewCommand}"/>
                        <RadioButton Content="Đơn Vị Tính" Tag="Scale" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenUnitViewCommand}"/>
                        <RadioButton Content="Nhà Cung Cấp" Tag="Factory" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenSupplierViewCommand}"/>
                        <RadioButton Content="Khách Hàng" Tag="AccountGroup" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenCustomerViewCommand}"/>

                        <TextBlock Text="HỆ THỐNG" Style="{StaticResource SidebarSectionHeader}"/>
                        <RadioButton Content="Người Dùng" Tag="AccountSettings" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenAppUserViewCommand}"
                                     Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <RadioButton Content="Đổi Mật Khẩu" Tag="KeyVariant" Style="{StaticResource SidebarItemStyle}" Command="{Binding OpenChangePasswordViewCommand}"/>
                        
                        <Border Height="20"/>
                    </StackPanel>
                </ScrollViewer>

                <!-- User Profile Footer -->
                <Border Grid.Row="2" Background="#F8F9FA" Padding="16,16" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="0,1,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="36" Height="36" CornerRadius="18" Background="{StaticResource PrimaryIndigoBrush}">
                            <TextBlock Text="{Binding CurrentUser.FullName[0]}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="10,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding CurrentUser.FullName}" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold" FontSize="13" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding CurrentUser.RoleCode}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                        </StackPanel>
                        <Button Grid.Column="2" Style="{StaticResource MaterialDesignIconButton}" Width="30" Height="30" Foreground="{StaticResource TextSecondaryBrush}" Command="{Binding LogoutCommand}">
                            <materialDesign:PackIcon Kind="Logout" Width="18" Height="18"/>
                        </Button>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Header -->
            <Border Background="White" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="0,0,0,1">
                <Grid Margin="24,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- View Title -->
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{Binding CurrentViewTitle, FallbackValue='DASHBOARD'}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="{Binding CurrentViewSubtitle, FallbackValue='Tổng quan hoạt động kinh doanh'}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>

                    <!-- Search Bar (Placeholder) -->
                    <Border Grid.Column="1" MaxWidth="400" Height="40" CornerRadius="8" Background="#F5F5F5" Margin="40,0" HorizontalAlignment="Left">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <materialDesign:PackIcon Kind="Search" Margin="12,0" VerticalAlignment="Center" Foreground="#888888"/>
                            <TextBox Grid.Column="1" materialDesign:TextFieldAssist.DecorationVisibility="Collapsed" BorderThickness="0" Background="Transparent" VerticalContentAlignment="Center" materialDesign:HintAssist.Hint="Tìm kiếm sản phẩm, đơn hàng..." FontSize="13"/>
                        </Grid>
                    </Border>

                    <!-- Notifications & Profile -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Style="{StaticResource MaterialDesignIconButton}" Foreground="#666666" Margin="0,0,8,0">
                            <materialDesign:PackIcon Kind="BellOutline"/>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignIconButton}" Foreground="#666666">
                            <materialDesign:PackIcon Kind="HelpCircleOutline"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Actual Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ContentControl Content="{Binding CurrentView}"/>
            </ScrollViewer>
        </Grid>

    </Grid>
</Window>

